proc main
  0 while true do
    // todo: this currently errors and shows that the current data on the stack is []
    //       maybe store the location of the last `drop` call and output it if the stack is empty?
    drop
  end
end