// note: there is already the same procedure from the standard library
proc factorial :: int -> int do
  1 while over 1 gt do
    over mul swap 1 sub swap
  end swap drop
end

proc main do
  "16! = " puts 16 factorial print
  // expected output: 20922789888000
end
