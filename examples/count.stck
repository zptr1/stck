include "linux"
include "std"

memory input 64 end

proc main
  "n = " puts
  64 input stdin read

  input over ptr+ 1 ptr- read8 '\n' eq if
    1 sub
  end

  input parse-int
  0 while 2dup gt do
    1 add dup print
  end drop drop
end
